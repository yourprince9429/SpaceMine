# SpaceMine - Flask Web应用

这是一个基本的Flask Web应用。

## 项目结构

- `app.py`: 主应用文件
- `requirements.txt`: 依赖文件

## 运行应用

1. 安装依赖：
   ```
   pip install -r requirements.txt
   ```

2. 运行应用：
   ```
   python app.py
